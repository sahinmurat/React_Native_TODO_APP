import React, { useEffect, useState } from 'react';
import { Text, SafeAreaView, StyleSheet, View, FlatList, Button, Image, Alert } from 'react-native';
import styles from '../../src/assest/styles'

/**
 * #303e45
 * #a7b6bd
 * #4a636e
 * #ff9b31
 */

const News = () => {
  
  return (

    <SafeAreaView style={{flex:1}}>
      <View style={{flex:1}} >
        <Text>aa</Text>
      </View>
    </SafeAreaView>
  )
}
export default News;

const styles = StyleSheet.create({

})

